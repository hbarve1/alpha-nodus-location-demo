export type TypeLocation = {
  address: string;
  // alias,
  // description,
  id: string;
  // managingOrganization,
  name: string;
  // npi,
  // partOf,
  status: string;
  // tag,
  // taxId,
  // telecom,
  // tenant,
  // type,
  updatedAt: number;
};
